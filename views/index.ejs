<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/styles.css" />
    <title>Agency List DB</title>
  </head>
  <body>
    <h1 style="color: red">MUTATOR</h1>
  <img id="logo" src="mutator-logo.png" alt="mutator-logo">
    <form class="fileForm" action="/upload" method="post" enctype="multipart/form-data">
      <h4 style="color: white">Wybierz plik txt który ma zostać przetworzony...</h4>
      <div>
        <input type="file" name="fileToUpload" />
        <button type="submit">Załaduj plik...</button>
      </div>
    </form>
    <div class="mailList">Check email which you want to send...</div>
    <div class="buttonWrapper">
      <button id="selectBtn">Copy Text</button>
      <button id="sendedBtn">Sended</button>
      <button id="clearBtn">Clear</button>
    </div>
    <table>
      <thead>
        <tr>
          <th>Nazwa Firmy</th>
          <th>Adres strony internetowej</th>
          <th>Adres Email</th>
          <th>Add</th>
          <th>Delete</th>
          <th>Uncheck</th>
        </tr>
      </thead>
      <tbody>
        <% if(firmy) {%> <% firmy.forEach(function(company){ %>

        <tr>
          <td><%- company.name %></td>
          <td>
            <%- `<a href="${company.www}" target="blank">${company.www}</a>` %>
          </td>
          <td class="companyEmail"><%- company.email %></td>
          <td class="buttonCell">
            <%- `<button type="button" id="${company.id}" class="checkBtn">
              Check</button
            >` %>
          </td>
          <td class="buttonCell">
            <%- `<button type="button" id="${company.id}" class="unCheckBtn">
              Uncheck</button
            >` %>
          </td>
          <td>
            <button class="deleteBtn" data-id="<%= company.id %>">
              Delete
            </button>
          </td>
        </tr>

        <%}); %> <%}%>
      </tbody>
    </table>

    <div id="copiedInfo">Text was copied to clipboard...</div>

    <script src="/js/jquery.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script type="text/javascript" src="/js/frontend.js"></script>
  </body>
</html>
